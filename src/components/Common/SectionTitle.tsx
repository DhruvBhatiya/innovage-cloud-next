import React from 'react'

const SectionTitle = (title: any) => {
    return (
        <h2 className="text-midnight_text text-4xl sm:text-5xl font-semibold pt-5 lg:pt-0 mb-10">{title}</h2>
    )
}

export default SectionTitle